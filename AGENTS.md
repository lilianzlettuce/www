# AGENTS

## Purpose
This file documents agent usage for this repository.

## Quick start
- Use Copilot Chat for code edits and debugging.
- Use the local dev command: `npm run dev`.

## Suggested agent modes
- `frontend`: make UI changes in `src/components` and `src/app`.
- `content`: edit MDX content in `content/projects`.
